library SafeMath{
    function sub(uint256 a, uint256 b) internal pure returns (uint256) {
        require(b <= a, "SafeMath: subtraction overflow");
        uint256 c = a - b;
        return c;
    }
}
contract Storage{
    using SafeMath for uint;
    uint public value1 = 5;
    uint public value2 = 10;

    function setValue1(uint256 num) public {
        if(num!=0){
            value1 = value1/num;
        }
    }
     function setValue2(uint256 amount) {
         value2 = value2-amount;
         value2 = value2.mul(amount);
     }
}